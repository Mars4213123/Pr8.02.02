<Window x:Class="Weather_Кантуганов.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Weather_Кантуганов" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <TextBox x:Name="txtCity" Width="120" Margin="0,0,10,0" Text="Пермь"/>
            <Button x:Name="btnSearch" Content="Поиск" Width="60" Margin="0,0,10,0" Click="SearchButton_Click"/>
            <Label Content="День:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="Days" Width="100" SelectionChanged="SelectDays"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,0">
            <Label x:Name="lblRequestCount" Content="Запросов: 0/50"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" Margin="10">
            <WrapPanel x:Name="parent" Orientation="Horizontal"/>
        </ScrollViewer>
    </Grid>
</Window>